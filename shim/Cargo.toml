[package]
name = "pingmybell-shim"
description = "Fail-open hook shim for PingMyBell — voice notifications for AI coding agents"
readme = "../README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true

[[bin]]
name = "pingmybell-shim"
path = "src/main.rs"

[dependencies]
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
