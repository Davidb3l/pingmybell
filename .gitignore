/target
# Scratch build dirs — agents use these to dodge cargo lock contention.
/target-*

node_modules/
dist/
src-tauri/gen/
src-tauri/binaries/
.DS_Store

# Hayvenhurst (added by `hayven init`) — generated index + installed skill
.hayven/
.claude/skills/
.agents/skills/
