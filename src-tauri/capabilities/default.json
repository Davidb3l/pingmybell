{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for PingMyBell windows",
  "windows": ["board", "overlay", "reply"],
  "permissions": ["core:default"]
}
